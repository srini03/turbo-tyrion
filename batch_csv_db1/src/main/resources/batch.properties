# Placeholders batch.*
#    for HSQLDB:
#batch.jdbc.driver=org.hsqldb.jdbcDriver
#batch.jdbc.url=jdbc:hsqldb:mem:testdb;sql.enforce_strict_size=true
# use this one for a separate server process so you can inspect the results
# (or add it to system properties with -D to override at run time).
# batch.jdbc.url=jdbc:hsqldb:hsql://localhost:9005/samples
#batch.jdbc.user=sa
#batch.jdbc.password=
#batch.schema=
#batch.schema.script=classpath:/org/springframework/batch/core/schema-hsqldb.sql

batch.jdbc.driver=com.mysql.jdbc.Driver
batch.jdbc.url=jdbc:mysql://localhost:3306/test
batch.jdbc.user=root
batch.jdbc.password=srini
batch.schema=
batch.schema.script1=classpath:/org/springframework/batch/core/schema-drop-mysql.sql
batch.schema.script2=classpath:/org/springframework/batch/core/schema-mysql.sql
batch.schema.script3=classpath:/sqls/schema-all-mysql.sql
